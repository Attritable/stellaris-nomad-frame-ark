# Nomad Frame Ark — potato origin
# Keys sourced from local Stellaris 4.4 + Giga 3.39.4 install slices under /workspace/stellaris-refs/
# (not invented). See KEYS.md.
#
# Vanilla Nomadic (00_origins.txt):
#   origin_default_nomads (Voidfarers) uses possible = { is_nomadic = yes } + playable has_nomads_dlc
#   is_nomadic = hardcoded country/government special requirement (NOT defined as scripted trigger
#               in 09_scripted_triggers_nomads.txt; that file only *uses* it)
#   has_nomads_dlc = { host_has_dlc = "Nomads" }  (09_scripted_triggers_nomads.txt)
#
# Giga Frame World (frameworld_origins.txt):
#   origin_frameworld — DISABLED in Giga 4.4 line (possible = { always = no } "Disabled for 4.0")
#   starting_colony = pc_giga_frameworld; initializers = giga_frameworld_system
#   This mod ships a SEPARATE origin (mobile ark), not a re-enable of origin_frameworld.

origin_nomad_frame_ark = {
	is_origin = yes
	icon = "gfx/interface/icons/origins/origins_default_nomads.dds"
	# TODO(art): custom picture GFX; reuse vanilla nomad default picture for potato
	picture = GFX_origin_nomad_default

	# Match other vanilla Nomadic origins (00_origins.txt origin_default_nomads et al.)
	non_colonizable_planet_class_neighbor = yes

	flags = {
		custom_start_screen
		nomad_frame_ark_origin
	}

	playable = {
		has_nomads_dlc = yes
	}
	# Potato: keep AI off (Frame Fix style). Voidfarers uses has_nomads_dlc for AI.
	ai_playable = { always = no }

	# Same special-requirement grammar as Voidfarers / Sacred Path / Heirs / Forever Cruise
	possible = {
		is_nomadic = yes
	}

	description = "origin_nomad_frame_ark_effects"

	advanced_start = yes

	# Do NOT set starting_colony = pc_giga_frameworld or initializers = giga_frameworld_system —
	# those are Frame World's stationary black-hole capital (giga_origin_frameworld.txt).
	# Nomadic capital is arkship-based (vanilla nomad_init initializers / arkship tiers).
	# Product TODO: whether to later blend Frame outpost strip loop — see QUESTIONS.md

	random_weight = {
		base = 0
	}
}
